**遥操作模拟不带摄像头**
目的：测试硬件环境，主从机械臂的连接，此命令用于启动 SO100 机器人的遥操作模式。

```plain

python lerobot/scripts/control_robot.py teleoperate  --robot-path lerobot/configs/robot/so100.yaml    --robot-overrides "~cameras"   --display-cameras 0
```

参数说明：

- `--robot-path lerobot/configs/robot/so100.yaml`：指定机器人的配置文件路径。
- `--robot-overrides "~cameras"`：禁用摄像头功能。
- `--display-cameras 0`：不显示摄像头画面。

pip install pynput

**采集数据：**

```plain
python lerobot/scripts/control_robot.py record    --robot-path lerobot/configs/robot/so100.yaml     --fps 30     --root data     --repo-id pick/so100_test    --tags pickbottle     --episode-time-s 30     --reset-time-s 6     --num-episodes 50     --push-to-hub 0
```

此命令用于记录 SO100 机器人的操作数据集。

参数说明：

- `--fps 30`：设置记录的帧率为每秒 30 帧。
- `--root data`：将数据保存到 `data` 目录。
- `--repo-id pick/so100_test`：指定数据集的标识符。
- `--tags pickbottle`：为数据集添加标签。
- `--episode-time-s 30`：每个记录片段的持续时间为 30 秒。
- `--reset-time-s 6`：每次重置之间的间隔时间为 6 秒。
- `--num-episodes 50`：总共记录 50 个片段。
- `--push-to-hub 0`：不将数据集上传到 Hugging Face Hub。
  **回放数据：**

```plain
python lerobot/scripts/control_robot.py replay     --robot-path lerobot/configs/robot/so100.yaml     --fps 30     --root data     --repo-id pick/so100_test     --episode 0     --robot-overrides "~cameras"
```

此命令用于回放指定的数据集片段。
参数说明：

- `--episode 0`：回放第 0 个片段。
- `--robot-overrides "~cameras"`：禁用摄像头功能
  **查看数据：**

```plain
python lerobot/scripts/visualize_dataset.py     --repo-id  pick/so100_test    --root ./data     --local-files-only 1     --episode-index 0
```

- **训练数据：**

```plain
set DATA_DIR=./data/   设置训练本地数据
export DATA_DIR=./data/
 python lerobot/scripts/train.py   dataset_repo_id=pick/so100_test    policy=act_so100_real     env=so100_real     hydra.run.dir=outputs/train/act_so100_test     hydra.job.name=act_so100_test     device=cuda     wandb.enable=true （同步日志到wandb需要翻墙）


 python3 lerobot/scripts/train.py   dataset_repo_id=pick/so100_test    policy=act_so100_real     env=so100_real     hydra.run.dir=outputs/train/act_so100_test     hydra.job.name=act_so100_test     device=cuda     wandb.enable=false
```

此命令用于训练控制 SO100 机器人的策略模型。

参数说明：

- `DATA_DIR=./data/`：指定数据集所在目录。
- `dataset_repo_id=pick/so100_test`：使用之前记录的数据集。
- `policy=act_so100_real`：指定策略配置。
- `env=so100_real`：指定环境配置。
- `hydra.run.dir=outputs/train/act_so100_test`：设置训练输出目录。
- `hydra.job.name=act_so100_test`：设置训练任务名称。
- `device=cuda`：使用 GPU 进行训练。
- `wandb.enable=true`：启用 Weights and Biases 进行训练监控。

**模型评估：**

```plain

python lerobot/scripts/control_robot.py record     --robot-path lerobot/configs/robot/so100.yaml     --fps 30    --root data     --repo-id pick/eval_so100_test     --tags pickbottle     --warmup-time-s 5     --episode-time-s 300     --reset-time-s 10     --num-episodes 2     -p outputs/train/act_so100_test/checkpoints/last/pretrained_model
```

此命令用于评估训练好的策略模型在 SO100 机器人上的表现。

参数说明：

- `--robot-path lerobot/configs/robot/so100.yaml`：指定机器人的配置文件路径。
- `--fps 30`：设置数据采集的帧率为每秒 30 帧。
- `--root data`：指定数据存储的根目录为 `data` 文件夹。
- `--repo-id pick/eval_so100_test`：设置评估数据集的标识符为 `pick/eval_so100_test`。
- `--tags pickbottle`：为评估数据集添加标签 `pickbottle`，便于分类和检索。
- `--warmup-time-s 5`：在每个回合开始前有 5 秒的预热时间。
- `--episode-time-s 300`：每个回合的持续时间为 300 秒（5 分钟）。
- `--reset-time-s 10`：每个回合之间的重置时间为 10 秒。
- `--num-episodes 2`：总共记录 2 个回合的评估数据。
- `-p outputs/train/act_so100_test/checkpoints/last/pretrained_model`：指定使用训练好的策略模型，该模型位于 `outputs/train/act_so100_test/checkpoints/last/pretrained_model`

## 只有学生端：手拖示教采集、训练和模型控制

如果没有 teacher/leader 端，可以使用学生端手拖示教模式。录制时程序会关闭 follower/student 机械臂扭矩，你需要用手扶住机械臂并拖动完成任务，避免机械臂突然下垂或碰撞。训练和模型控制仍然使用原来的 LeRobot 数据格式。

进入项目目录：

```powershell
cd C:\Users\Administrator\Desktop\robotarm\genkiarm\genkiarm
conda activate hostprotocol
$env:PYTHONUTF8="1"
```

校准学生臂：

```powershell
python lerobot/scripts/control_robot.py calibrate `
  --robot-path lerobot/configs/robot/so100_student.yaml `
  --arms main_follower
```

手拖采集数据：

```powershell
python lerobot/scripts/control_robot.py record `
  --robot-path lerobot/configs/robot/so100_student.yaml `
  --record-control-mode kinesthetic `
  --fps 30 `
  --root data `
  --repo-id pick/so100_student `
  --tags pickbottle `
  --warmup-time-s 5 `
  --episode-time-s 30 `
  --reset-time-s 6 `
  --num-episodes 50 `
  --kinesthetic-gripper-keyboard 1 `
  --gripper-open-angle 90 `
  --gripper-closed-angle -90 `
  --gripper-open-key o `
  --gripper-close-key c `
  --gripper-speed 30 `
  --push-to-hub 0

python lerobot/scripts/control_robot.py record --robot-path lerobot/configs/robot/so100_student.yaml --record-control-mode kinesthetic --fps 30 --root data --repo-id pick/so100_student --tags pickbottle --warmup-time-s 5 --episode-time-s 30 --reset-time-s 6 --num-episodes 50 --kinesthetic-gripper-keyboard 1 --gripper-open-angle 90 --gripper-closed-angle -90 --gripper-open-key o --gripper-close-key c --gripper-speed 30 --push-to-hub 0

set PYTHONUTF8=1
```

录制时长按 `O` 打开夹爪，长按 `C` 关闭夹爪，松开即停止；大小写都可以。启用 `--kinesthetic-gripper-keyboard 1` 后，大臂关节会关闭扭矩用于手拖，`gripper` 夹爪电机会单独保持扭矩开启并按 `--gripper-speed` 连续改变目标角度。`--gripper-open-angle` 和 `--gripper-closed-angle` 使用参考脚本里的舵机角度域 `-90..90`，不是 LeRobot 校准后的 `0..180` 位置值；程序会把 `-90/0/90` 映射为原始电机位置 `1024/2048/3072` 后写入夹爪。每帧仍记录真实的 `Present_Position`，所以第 6 维 `action` 会自然包含夹爪开合轨迹。如果实际夹爪方向相反，不需要改代码，交换 `--gripper-open-angle` 和 `--gripper-closed-angle` 两个参数即可。

查看数据：

```powershell
python lerobot/scripts/visualize_dataset.py `
  --repo-id pick/so100_student `
  --root ./data `
  --local-files-only 1 `
  --episode-index 0
```
python lerobot/scripts/visualize_dataset.py --repo-id pick/so100_student --root ./data --episode-index 0

回放数据：

```powershell
python lerobot/scripts/control_robot.py replay `
  --robot-path lerobot/configs/robot/so100_student.yaml `
  --fps 30 `
  --root data `
  --repo-id pick/so100_student `
  --episode 0 `
  --robot-overrides "~cameras"
```
```powershell
python lerobot/scripts/control_robot.py replay --robot-path lerobot/configs/robot/so100_student.yaml --fps 30 --root data --repo-id pick/so100_student --episode 0 --robot-overrides "~cameras"
```

此命令用于只连接学生端机械臂回放第 0 轮采集数据。`--robot-overrides "~cameras"` 表示回放时禁用摄像头，只让机械臂按数据里的 `action` 轨迹运动。

训练模型：

```powershell
$env:DATA_DIR="./data"
python lerobot/scripts/train.py `
  dataset_repo_id=pick/so100_student `
  policy=act_so100_real `
  env=so100_real `
  hydra.run.dir=outputs/train/act_so100_student `
  hydra.job.name=act_so100_student `
  device=cuda `
  wandb.enable=false

python lerobot/scripts/train.py dataset_repo_id=pick/so100_student policy=act_so100_real env=so100_real hydra.run.dir=outputs/train/act_so100_student hydra.job.name=act_so100_student device=cuda wandb.enable=false
```

让模型控制真实学生臂：

```powershell
python lerobot/scripts/control_robot.py record `
  --robot-path lerobot/configs/robot/so100_student.yaml `
  --fps 30 `
  --root data `
  --repo-id pick/eval_so100_student `
  --tags pickbottle `
  --warmup-time-s 5 `
  --episode-time-s 300 `
  --reset-time-s 10 `
  --num-episodes 2 `
  --push-to-hub 0 `
  -p outputs/train/act_so100_student/checkpoints/last/pretrained_model

python lerobot/scripts/control_robot.py record --robot-path lerobot/configs/robot/so100_student.yaml --fps 30 --root data --repo-id pick/eval_so100_student --tags pickbottle --warmup-time-s 5 --episode-time-s 300 --reset-time-s 10 --num-episodes 2 --push-to-hub 0 -p outputs/train/act_so100_student/checkpoints/last/pretrained_model
```

```
